class User:

    def __init__(self, id, name, token, type, follower, followee, answer, like, collect, question, article,
                 think):
        self.id = id
        self.name = name
        self.token = token
